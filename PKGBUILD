# Maintainer: Sebastien MacDougall-Landry

pkgname=tidal-engine
pkgver=0.1
pkgrel=1
pkgdesc='Lua game engine'
url='https://github.com/EmperorPenguin18/tidal/'
source=("https://github.com/EmperorPenguin18/tidal/archive/$pkgver.tar.gz")
arch=('x86_64')
license=('LGPL3')
depends=('moonvulkan' 'lua-sdl2')
sha256sums=('')

build () {
  cd "$srcdir/$pkgname-$pkgver"
  #
}

package () {
  cd "$srcdir/$pkgname-$pkgver"
  #
}
